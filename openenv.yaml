name: studentbuddy
description: Student task management environment
version: 1.0

observation_space:
  tasks: list
  time_left: int
  stress: float

action_space:
  action_type: string
  task_title: string
  time_spent: int

reward: continuous